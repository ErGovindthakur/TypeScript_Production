let drink = "chai";

let cups = Math.random() > 0.5 ? 10: 5;

// since ts automatically detects type of variables with explicit define it, called type inference.
let channelName = "Chai aur Code";
// channelName = 1221;

// 1. type annotation
let myName : string = "Govind Thakur";
myName = "Er Govind Thakur";

let isTrue : boolean = true;
console.log(isTrue);